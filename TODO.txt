
L'AJOUT DE COMMENTAIRE 

Faire des tableaux de tableaux [] []
voir avec le sessionStorage



Création d'un tableaux multiple => let myTab = [ [][], [][], [][] ]
				=> le 1er tableau = nom
				=> le 2eme tableau = commentaire (étoile + commentaire)

Au moment de l'affichage des commentaires dans la classe : Resto (ligne 19 ou directement dans displayComment()) => On charge aussi le nouveau tableau
				=> on fait une boucle pour la longueur du tableau myTab
				=> on compare le myTab[i] avec le this.name
				=> si correspondace alors on affiche le myTab[i][]



Revoir l'affichage des nouveaux commentaires
Revoir surtout l'affichage des moyennes des commentaires

Voir les fonctions checkBox, celà n'affiche pas mes nouveaux restorants créer en cours de session.




















REVOIR LA MISE A JOUR DE LA CARTE EN FONCTION DES FILTRES !! car elle ne se fait pas !





pour la map voir avec la fonction : getBounds();
https://www.weboblog.fr/recuperer-les-limites-de-la-carte-getbounds-v3
https://developers.google.com/maps/documentation/javascript/events
https://developers.google.com/maps/documentation/android-sdk/views?hl=fr

// Vérifiez quels restaurants locaux sont sur la carte et ajoutez UNIQUEMENT ces restaurants sur le tableau //
    this.google.maps.event.addListenerOnce(this.map, 'idle', () => {
        const filteredLocalRestaurants = this.restaurants.filter((element) => this.map.getBounds().contains({ lat: element.lat, lng: element.long }));
        this.restaurants = filteredLocalRestaurants;
      });


LA PARTIE 3 DU PROJET